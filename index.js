function greetingsMsg(firstname, language) {
    if (language === "English") {
      return "Hello, " + firstname;
    }else if (language === "Setswana") {
      return "Dumela, " + firstname;
    }else if (language === "IsiXhosa") {
      return "Molo, " + firstname;
    }
}
